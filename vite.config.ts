import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  base: "./", // Set base path for relative asset loading
  // You can add other Vite configurations here later if needed
}) 